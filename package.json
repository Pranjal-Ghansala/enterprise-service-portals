{
  "name": "enterprise-service-portal",
  "version": "1.0.0",
  "description": "Full-stack enterprise service portal",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "22.22.0"
  }
}